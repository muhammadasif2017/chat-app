# Baked into the frontend image at build time — set before running docker compose up --build
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_WS_URL=https://api.yourdomain.com

# Redis auth — also set REDIS_URL in backend/.env as redis://:password@redis:6379
REDIS_PASSWORD=change-me-strong-password
